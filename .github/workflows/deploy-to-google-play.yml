name: Deploy to Google Play

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build & Release
    runs-on: ubuntu-latest
    
    defaults:
      run:
        working-directory: ./

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Decode Keystore
        env:
          KEYSTORE_FILE: ${{ secrets.KEYSTORE_FILE }}
        run: |
          echo $KEYSTORE_FILE | base64 --decode --ignore-garbage > keystore.jks

      - name: Inject Build Secrets
        env:
          BUGSNAG_API_KEY: ${{ secrets.BUGSNAG_API_KEY }}
          ADMOB_APP_ID: ${{ secrets.ADMOB_APP_ID }}
          ADMOB_BANNER_AD_UNIT_ID: ${{ secrets.ADMOB_BANNER_AD_UNIT_ID }}
          ADMOB_NATIVE_AD_UNIT_ID: ${{ secrets.ADMOB_NATIVE_AD_UNIT_ID }}
        run: |
          echo "bugsnag.api.key=$BUGSNAG_API_KEY" >> local.properties
          echo "admob.app_id=$ADMOB_APP_ID" >> local.properties
          echo "admob.banner_ad_unit_id=$ADMOB_BANNER_AD_UNIT_ID" >> local.properties
          echo "admob.native_ad_unit_id=$ADMOB_NATIVE_AD_UNIT_ID" >> local.properties

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build Release Bundle
        run: ./gradlew :app:bundleRelease --stacktrace

      - name: Sign Bundle
        uses: r0adkll/sign-android-release@v1
        id: sign_app
        with:
          releaseDirectory: app/build/outputs/bundle/release
          signingKeyBase64: ${{ secrets.KEYSTORE_FILE }}
          alias: ${{ secrets.KEY_ALIAS }}
          keyStorePassword: ${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword: ${{ secrets.KEY_PASSWORD }}
        env:
          BUILD_TOOLS_VERSION: "34.0.0"

      - name: Upload to Google Play (Internal)
        if: github.ref == 'refs/heads/main'
        uses: r0adkll/upload-google-play@v1
        with:
          serviceAccountJsonPlainText: ${{ secrets.SERVICE_ACCOUNT_JSON }}
          packageName: com.allensandiego.notepad
          releaseFiles: ${{ steps.sign_app.outputs.signedReleaseFile }}
          tracks: internal
          status: completed
          inAppUpdatePriority: 2